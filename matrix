#include <iostream>
#include <fstream>
using namespace std;

double ** inputmf(ifstream &f, int N, char s[40])
{
int i, int j;
double **a
a=new double *[n];
if(a==NULL){cout<<"\n f";
exit(1);
}
 void outputmf(ofstream &f, double **a, int N, char s[40])
{
    int i, int j;
    f.open("C:\Users\User\Desktop\9 11 10\matrix\out.txt");

}

int main(void)
{


const int N = 3;
   int matrix[N][N]
f.open("C:\Users\User\Desktop\9 11 10\matrix\in.txt");
f.close();
return a;
 int tmp[N][N];
    for(int i = 0; i < N; i++)

        for(int j = 0; j < N; j++)
            tmp[i][j] = matrix[N-j-1][i];
    for(int i = 0; i < N; i++)
    {
        for(int j = 0; j < N; j++)
            cout << tmp[i][j] << ' ';
